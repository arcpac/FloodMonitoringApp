class Unit {
  constructor(id, name, image, status, active) {
    this.id = id;
    this.name = name;
    this.image = image,
    this.status = status;
    this.active = active;
  }
}

export default Unit;
